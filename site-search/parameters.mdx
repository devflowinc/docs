<ParamField path="datasetId" type="string" description="The id of the dataset used to search" required>
    The id of the dataset used to search
</ParamField>
<ParamField path="apiKey" type="string" description="The api key used to search" required>
    The api key used to search
</ParamField>
<ParamField path="chat" type="boolean" description="Adds AI search to the compomnent. Only works in the Modal version" defaut="true">
    Adds AI search to the compomnent. Only works in the Modal version
</ParamField>
<ParamField path="analytics" type="boolean" description="If you want to track the searches and clicks" defaut="true">
    If you want to track the searches and clicks
</ParamField>
<ParamField path="placeholder" type="string" description="Placeholder for the search input" defaut="Search...">
    Placeholder for the search input
</ParamField>
<ParamField path="onResultClick" type="function" description="Any function you want to run when a user clicks on a search result" defaut="() => {}">
    Any function you want to run when a user clicks on a search result
</ParamField>
<ParamField path="theme" type="light | dark" description="Color theme" defaut="light">
    Color theme
</ParamField>
<ParamField path="searchOptions" type="SearchChunksReqPayload" description="Any options you want to pass to the search function" defaut="{ search_type='hybrid' }">
    Any options you want to pass to the search function
</ParamField>
<ParamField path="openKeyCombination" type="{ key?=string; label?=string; ctrl?=boolean }[]" description="Key combination to open the search" defaut="[{ ctrl=true } { key='k' label='K' }]">
    Key combination to open the search
</ParamField>
<ParamField path="ButtonEl" type="JSX.ElementType" description="The element to use as the button defaults to a rectangular input if null" default="null">
    The element to use as the button defaults to a rectangular input if null
</ParamField>
<ParamField path="responsive" type="boolean" description="If you want the search bar to be responsive and shrink on mobile" default="false">
    If you want the search bar to be responsive and shrink on mobile
</ParamField>
<ParamField path="suggestedQueries" type="boolean" description="If you want to show suggested queries and allow the user to refresh them" default="true">
    If you want to show suggested queries and allow the user to refresh them
</ParamField>
<ParamField path="defaultSearchQueries" type="string[]" description="The default search queries to show in search mode" default="[]">
    The default search queries to show in search mode
</ParamField>
<ParamField path="defaultAiQuestions" type="string[]" description="The default AI questions to show in chat mode" default="[]">
    The default AI questions to show in chat mode
</ParamField>
<ParamField path="brandLogoImgSrcUrl" type="string" description="The url of the image to use as the brand logo" default="null">
    The url of the image to use as the brand logo
</ParamField>
<ParamField path="brandName" type="string" description="The name of the brand to show in the search bar" default="null">
    The name of the brand to show in the search bar
</ParamField>
<ParamField path="problemLink" type="string" description="The link to the page where the user can report a problem when no results are found" default="mailto:help@trieve.ai?subject=">
    The link to the page where the user can report a problem when no results are found
</ParamField>
<ParamField path="brandColor" type="string" description="The color to use as the accent color" default="#CB53EB">
    The color to use as the accent color
</ParamField>

